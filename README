lex/yacc grammar from http://www.quut.com/c/ANSI-C-grammar-l.html

to run file download flex-bison from https://sourceforge.net/projects/winflexbison/

win_flex -d filename allows to debug flex text matching but -d is optional
win_bison -d filename is necessary to generate cp.tab.h file.

To run code on linux machines:

cc -c cp.tab.c lex.yy.c inc.c
cc -o cparse cp.tab.o lex.yy.o inc.o
./cparse filename

Targets:

Make code neater - S
Search through include files - P
Brackets - S
Movement through code not simulated - S

Editing

Voice
